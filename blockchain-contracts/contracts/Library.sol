// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

library Library {
    // Define enum 'State' with the following values:
    enum State {
        CREATED,
        FOR_SALE,
        NOT_AVAILABLE,
        ON_DELIVERY,
        RECEIVED
    }
}